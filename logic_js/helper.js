class Helper {
	var QUERY_TYPES = new Enum(['TIME', 'DEVICE']);
	/**
	Takes 2 arguments:
	- Query Type: either by Time or Device which corresponds to the enum above
	- Query arguments
	Returns: [Push]
	**/
	filter(){

	}

}